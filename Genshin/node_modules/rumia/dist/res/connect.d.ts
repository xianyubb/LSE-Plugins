import { UrlProvider, Event } from "reconnecting-websocket";
export declare class WebSocket {
    private socket;
    onopen: null | ((event: Event) => void);
    onmessage: null | ((msg: any) => void);
    constructor(url: UrlProvider, maxRetries: number);
    send(data: any): void;
}
